<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Vocagraphy!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="header">
      <div class="top-bar">
        <div class="top-bar-left">
          <%= link "Home", to: page_path(@conn, :index), class: "menu-text" %>
        </div>
        <div class="top-bar-right">
          <ul class="menu">
            <%= if @current_user do %>
              <li><%= @current_user.username %>
              <li><%= link "My Videos", to: video_path(@conn, :index) %></li>
              <li>
                <%= link "Log out",
                to: session_path(@conn, :delete, @current_user), method: "delete" %>
              </li>
            <% else %>
              <li><%= link "Register", to: user_path(@conn, :new) %></li>
              <li><%= link "Log in", to: session_path(@conn, :new) %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <% info  = get_flash(@conn, :info) %>
    <% error = get_flash(@conn, :error) %>

    <%= if info do %>
      <div class="callout primary" role="alert"><%= info %></div>
    <% end %>

    <%= if error do %>
      <div class="callout alert" role="alert"><%= error %></div>
    <% end %>

    <main role="main">
      <%= render @view_module, @view_template, assigns %>
    </main>

    <script>window.userToken = "<%= assigns[:user_token] %>"</script>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
